{% extends "base.html.twig" %}

{% block body %}
	<h4> Ajouter un article : </h4>
	{{ form_start(form) }}
		{{form_rest(form)}}
		<button type="submit" class="btn">Ajouter</button>
	{{ form_end(form)}}
{% endblock %}